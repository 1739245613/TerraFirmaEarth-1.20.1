# 数据包兼容写法教程

## 目的

本文专门说明本项目里“与 `1.20 TFC` 原版不同”的数据包 / KubeJS / 第三方扩展写法。

- 没写在本文里的内容，默认仍按 `1.20 TFC` 原版思路接入。
- `AFC` 不是兼容基线，也不是推荐参考样板。
- 本文只记录“当前项目已经稳定存在的偏离点”和“正确接法”。

## 总原则

先记住一条最重要的规则：

- 普通地物、普通果树、普通植物、普通 biome 装饰，默认优先继续按原版 `TFC` 标签链扩展。
- 只有森林树主入口、部分 `1.21` 气候字段、部分生物气候判定，是当前项目的特例。

换句话说：

- 能按原版写的，继续按原版写。
- 只有确实需要接入本项目移植过来的 `1.21` 语义时，才使用本文里的特例写法。

## 一、普通地物仍按原版写

下面这些内容，默认仍然优先按 `1.20 TFC` 原版方式扩展：

- 普通果树
- 野生作物
- 浆果灌木
- 陆地植物
- 海洋植物
- 岸边装饰
- 普通 placed feature

推荐入口仍然是：

- `data/tfc/tags/worldgen/placed_feature/feature/*.json`

例如：

- 果树加到 `data/tfc/tags/worldgen/placed_feature/feature/fruit_trees.json`
- 陆地植物加到 `data/tfc/tags/worldgen/placed_feature/feature/land_plants.json`
- 海洋植物加到 `data/tfc/tags/worldgen/placed_feature/feature/ocean_plants.json`
- 岸边装饰加到 `data/tfc/tags/worldgen/placed_feature/feature/shore_decorations.json`

示例，向果树分类追加一个 placed feature：

```json
{
  "replace": false,
  "values": [
    "yourmod:plant/my_fruit_tree",
    "yourmod:plant/my_fruit_tree_sparse"
  ]
}
```

### 这一层为什么仍然推荐原版写法

因为当前项目已经把 `data/tfc/tags/worldgen/placed_feature/feature/*.json` 这一层收回到可合并语义，第三方追加项不会再被整表盖掉。

### 不推荐直接拿 biome 阶段标签当通用入口

虽然原版 `TFC` 也有：

- `data/tfc/tags/worldgen/placed_feature/in_biome/<stage>/<biome>.json`

但当前项目里仍有不少 biome 阶段标签是项目自管的 `replace: true` 整表替换写法，所以这层不适合当“通用第三方兼容入口”。

例如当前仍属于项目主控的就包括：

- `data/tfc/tags/worldgen/placed_feature/in_biome/surface_decoration/plains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/plains.json`

因此建议：

- 只想让第三方地物正常并入现有体系时，优先扩展 `feature/*.json`
- 只有明确想整体接管某个 biome 某个阶段时，才改 `in_biome/...`

## 二、森林树是本项目的特例入口

森林树和普通地物不同，因为它不是简单“往 biome 里塞一个 placed feature”，而是先经过森林主特征统一调度，再由主特征决定：

- 当前区块是什么森林类型
- 树密度是多少
- 允许出现几种树
- 是否偏向 old growth / dead / krummholz
- 是否使用 `groundwater / rain variance / elevation / soil_disc`

### 当前项目为什么和原版不一样

当前项目已经把默认森林主入口从原版 `tfc:forest` 换成了 `tfe:forest_121`，对应文件是：

- `data/tfc/worldgen/configured_feature/forest.json`

因此，想把树接入“默认森林主链”，不能再只看原版 `tfc:forest`，而要接当前项目实际使用的树入口标签：

- `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json`

注意：这不代表公共 `data/tfc/worldgen/configured_feature/tree/*_entry.json` 也应被改成本项目的 entry 类型。公共 `tfc:tree/*_entry` 仍应保持原版 / 第三方可读的 `tfc:forest_entry` 形态；本项目自己的 1.21 树 entry 放在 `data/tfe/worldgen/configured_feature/tree/*_entry.json`。

### 正确接法

1. 先写一个树 entry 的 configured feature
2. 再把这个 entry 的 id 追加到 `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json`

标签示例：

```json
{
  "replace": false,
  "values": [
    "yourmod:tree/my_tree_entry"
  ]
}
```

### 当前森林树支持两种 entry 写法

#### 写法 A：原版风格 entry

如果你只需要原版那种基于温度 / 降雨的森林树筛选，可以继续写原版风格的 tree entry：

```json
{
  "type": "tfc:forest_entry",
  "config": {
    "climate": {
      "min_temperature": 0.0,
      "max_temperature": 18.0,
      "min_rainfall": 180.0,
      "max_rainfall": 400.0
    },
    "normal_tree": "yourmod:tree/my_tree",
    "dead_tree": "yourmod:tree/my_tree_dead"
  }
}
```

这种写法的特点是：

- 更接近原版 `1.20 TFC`
- 不使用本项目新增的 `1.21` 森林语义
- 可以被当前项目的森林主链继续读取

#### 写法 B：本项目扩展的 `1.21` 风格 entry

如果你想接入当前项目移植过来的 `1.21` 森林筛选语义，就写：

- `type: "tfe:forest_entry_121"`

示例：

```json
{
  "type": "tfe:forest_entry_121",
  "config": {
    "climate": {
      "min_temperature": 2.0,
      "max_temperature": 18.0,
      "min_groundwater": 180.0,
      "max_groundwater": 420.0,
      "min_rain_variance": -0.4,
      "max_rain_variance": 0.7,
      "rain_variance_absolute": false
    },
    "normal_tree": "yourmod:tree/my_tree",
    "dead_tree": "yourmod:tree/my_tree_dead",
    "old_growth_tree": "yourmod:tree/my_tree_large",
    "soil_disc": "tfc:alfisol_disc"
  }
}
```

这种写法比原版多出来的语义主要有：

- `min_groundwater`
- `max_groundwater`
- `min_rain_variance`
- `max_rain_variance`
- `rain_variance_absolute`
- `min_elevation`
- `max_elevation`
- `soil_disc`

### 什么时候选哪一种

- 想尽量保持原版 `1.20 TFC` 风格：优先写 `tfc:forest_entry`
- 想吃到当前项目移植过来的 `1.21` 森林视觉和森林筛树逻辑：写 `tfe:forest_entry_121`

### 非森林果树不要写进这里

像椰子树这种“非森林主链的果树 / 棕榈类 / 单独 placed feature”，不要写进 `forest_121_trees`。

它们仍应优先走普通地物入口，例如：

- `data/tfc/tags/worldgen/placed_feature/feature/fruit_trees.json`

## 二点五、`AFC` 联动下的森林强覆盖特判

这一节不是通用第三方入口，而是当前项目为了修复 `AFC` 会把默认森林链整体盖掉而保留的专项兼容兜底。

先记住最重要的规则：

- 如果你想在数据包层“覆盖掉另一个 mod 已经占用的同一份资源 id”，包内文件必须使用同一个命名空间和同一条相对路径。
- 也就是说，真正决定是否算“同一个资源”的，不是外层目录叫不叫 `tfe_server_data_overrides`，而是包内最终是否落在同一个 `data/<namespace>/...` 路径。

例如当前项目要压过 `AFC` 的：

- `tfc:forest`

对应包内文件就必须是：

- `data/tfc/worldgen/configured_feature/forest.json`

如果你改成：

- `data/tfe/worldgen/configured_feature/forest.json`

那它就不再是同一个资源 id，而是变成一个新的 `tfe:forest`，自然覆盖不了 `AFC` 的 `tfc:forest`。

### 当前实现里，哪些路径是“可改名”的，哪些不是

- `src/main/resources/tfe_server_data_overrides`
  - 这是当前项目内置兼容包的外层资源目录名。
  - 它不是硬性必须叫这个名字；只要 [`NTEBuiltinPackEvents.java`](C:/Users/g1739/Desktop/MCmod源码/群峦地形移植/src/main/java/com/newterraearth/tfe/event/NTEBuiltinPackEvents.java) 里改成指向别的目录，并且新目录下仍然有 `pack.mcmeta`，就能工作。
- `pack.mcmeta`
  - 这是独立数据包根目录必须有的元数据文件。
- `data/tfc/...`
  - 这部分才是本次覆盖真正不能随便改的地方。
  - 只要目的是继续压过 `AFC` 当前那批共享森林资源，路径就必须保持同 namespace、同相对路径。
- `data/tfe/...`
  - 这部分不是为了压过 `AFC` 的同资源 id，而是当前项目自己的森林树池和适配 entry。
  - 当前内置包会在这里追加一组 `tfe:tree/afc/*_entry`，用于把 AFC 1.20 实际存在的树按 1.21 气候字段接入本项目森林链。

### 当前项目的 `AFC` 森林强覆盖具体文件

当前项目内置兼容包只在检测到 `afc` 已加载时启用，并且只强覆盖下面这 `30` 个文件：

- `data/tfc/worldgen/configured_feature/forest.json`
- `data/tfc/worldgen/configured_feature/tree/acacia_large.json`
- `data/tfc/worldgen/configured_feature/tree/ash.json`
- `data/tfc/worldgen/configured_feature/tree/kapok.json`
- `data/tfc/worldgen/configured_feature/tree/pine_large.json`
- `data/tfc/worldgen/configured_feature/tree/sequoia.json`
- `data/tfc/worldgen/configured_feature/tree/sequoia_large.json`
- `data/tfc/worldgen/configured_feature/tree/spruce.json`
- `data/tfc/worldgen/configured_feature/tree/spruce_large.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/badlands.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/canyons.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/hills.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/low_canyons.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/lowlands.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/mountain_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/mountains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/oceanic_mountain_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/oceanic_mountains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/old_mountain_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/old_mountains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/plains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/plateau_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/plateau.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/river.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/rolling_hills.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/volcanic_mountain_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/volcanic_mountains.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/volcanic_oceanic_mountain_lake.json`
- `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/volcanic_oceanic_mountains.json`

其中 8 个 `tree/*.json` 是 AFC 同路径树配置配套特判：AFC 会替换这些树的结构模板或树体配置，启用 AFC 时必须让 configured feature 与 AFC 自己的模板同源，避免混用当前项目 / TFC 配置后出现截断、秃顶或裸露树干。`tree/sequoia_large.json` 是最明显的例子：AFC 会替换 `tfc:sequoia_large/layer*` 结构模板并新增 `layer4_*` 顶盖，所以混用三层 TFC 配置会在树顶留下平整裸露原木。

同一个内置包还会追加 `data/tfe` 命名空间内容，但这些不是“强覆盖 AFC 同路径资源”：

- `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json`
- `data/tfe/worldgen/configured_feature/tree/afc/*_entry.json`

这组 `tfe:tree/afc/*_entry` 是适配层：树体、木材、落叶和灌木引用沿用 AFC 1.20 实际存在的资源，气候筛选字段按 AFC 1.21 同名 entry 的 `groundwater / rain_variance / elevation` 口径改写。它不会加入 AFC 1.21 新增但 AFC 1.20 不存在的树，例如 `dawn_redwood`。

### 什么时候才应该参考这套写法

- 只有当你明确需要“把另一个 mod 已经盖掉的共享森林入口重新拿回来”，才参考这套强覆盖写法。
- 如果你只是正常追加第三方地物、第三方树种或 biome 装饰，不要直接学这一节，仍然优先按前面的普通入口或森林树入口接。

### 风险边界

- 这套写法本质上是 `SERVER_DATA + Pack.Position.TOP` 的专项兼容特判。
- 只要 `afc` 没加载，这个兼容包就不会启用。
- 一旦 `afc` 已加载，这 `30` 个同路径文件上的第三方改动都会排在当前项目这个兼容包后面。
- 因此如果后续有整合包作者或用户确实想改这 `30` 个文件，最好把这层兼容做成可配置开关，而不是继续扩大强覆盖范围。
- `data/tfe/worldgen/configured_feature/tree/afc/*_entry.json` 属于当前项目自己的 AFC 适配树池，不会压过 `afc` 的同路径资源；但如果你也向 `#tfe:forest_121_trees` 追加同类树，应避免重复塞入同一树种。

### 不必把它理解成“正常写法失效”

- 上面这条风险边界，不等于第三方 `KubeJS / datapack` 的常规新增写法整体失效。
- 只要是按本教程默认推荐的方式做“新增自己的 configured feature / placed feature / tree entry，再追加到公共标签，且公共标签保持 `replace: false`”：
  - 普通情况下仍然可以正常生效。
- 真正会被当前 `AFC` 专项兼容层压住的，主要还是直接去改那 `30` 个同路径总入口文件的写法。
- 因此对用户来说，可以继续把本教程前面那些“普通地物新增”“森林树新增”“普通刷怪 / fauna 接法”理解成默认可用的正常扩展路径，不需要因为这一节的强覆盖特判就额外担心。

### `KubeJS / datapack` 在这 `30` 个文件上的实际生效边界

先区分两类改法：

- 改“入口本身”
- 改“入口下面挂的内容”

当前这 `30` 个文件里：

- `data/tfc/worldgen/configured_feature/forest.json`
  - 这是森林主 configured feature 入口，不是 biome 文件。
- 8 个 `data/tfc/worldgen/configured_feature/tree/*.json`
  - 这是 TFC 公共树 configured feature；本项目只在 `afc` 已加载时强覆盖 `acacia_large / ash / kapok / pine_large / sequoia / sequoia_large / spruce / spruce_large`，用来匹配 AFC 同路径树配置和结构模板。
- 另外 `21` 个 `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/*.json`
  - 这是各类地形 / biome 类型在 `large_features` 阶段引用的 placed feature 标签，也不是 biome 本体 json。
  - 真正的 biome 文件会像 `data/tfc/worldgen/biome/volcanic_mountains.json` 这样，在 `features` 里引用 `#tfc:in_biome/large_features/volcanic_mountains`。

因此：

- 如果你用 `KubeJS` 或第三方 datapack 直接改这 `30` 个同路径入口文件：
  - 在 `afc` 已加载的前提下，当前项目这层顶层兼容会压过你的改动。
- 如果你改的是这些入口下面继续引用的普通标签或 feature：
  - 仍然通常可以生效，因为当前项目没有把那些下游内容一起强覆盖掉。

最典型的例子：

- 想改默认森林树池：
  - 不要去抢 `data/tfc/worldgen/configured_feature/forest.json`
  - 应优先扩展 `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json`
- 想给火山大地物再挂一些火山相关 feature：
  - 不要去抢 `data/tfc/tags/worldgen/placed_feature/in_biome/large_features/volcanic_mountains.json`
  - 应优先扩展它下面继续引用的 `data/tfc/tags/worldgen/placed_feature/feature/volcanoes.json`
- 想给火山区再补湖泊类附加地物：
  - 可以优先扩展 `data/tfc/tags/worldgen/placed_feature/feature/surface_flood_fill_lakes.json`

可以把它简单理解成：

- 改这 `30` 个“总入口”，当前 `AFC` 兼容层会接管优先级。
- 改总入口下面挂的“子分类 / 子标签 / 具体 placed feature”，大多数情况下仍然能并入现有链路。

### 作为例子的扩展路径对照表

下面这张表，可以直接当作“平时该怎么接、哪些路径不要默认去抢”的速查例子：

| 场景 | 推荐位置 / 做法 | 会不会被当前 `AFC` 强覆盖影响 | 备注 |
| --- | --- | --- | --- |
| 新建自己的 `configured_feature / placed_feature` | 先定义你自己的资源 id | 不会 | 这只是地物本体，不是在抢入口 |
| 普通地物新增 | 追加到 `data/tfc/tags/worldgen/placed_feature/feature/*.json` | 基本不会 | 最推荐的常规写法 |
| 火山类地物新增 | 追加到 `data/tfc/tags/worldgen/placed_feature/feature/volcanoes.json` | 不会 | 活火山额外挂物走这里 |
| 湖泊 / 洪泛类附加地物新增 | 追加到 `data/tfc/tags/worldgen/placed_feature/feature/surface_flood_fill_lakes.json` | 不会 | 火山区补湖泊类内容走这里 |
| 森林树新增 | 追加到 `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json` | 不会 | 树走这里，不去抢 `tfc:forest` |
| 改公共 `tfc:tree/*_entry` | 不推荐 | 可能影响第三方 | 这些 entry 会被 `tfc_structures` 等模组按原版 `tfc:forest_entry` 读取 |
| 生物新增 / 刷怪 / fauna | 走 biome 刷怪或 fauna 链 | 不会 | 和这 `30` 个森林 / 树 worldgen 入口不是一条链 |
| 改公共子标签里的内容 | 可以，但尽量别整表 `replace: true` | 一般不会被这次 `AFC` 层压住 | 但仍可能和别的 mod / 数据包产生兼容冲突 |
| 直接改 `data/tfc/worldgen/configured_feature/forest.json` | 不推荐 | 会 | 这是森林总入口 |
| 直接改那 8 个 `data/tfc/worldgen/configured_feature/tree/*.json` | 不推荐 | 会 | AFC 联动下这些是树配置 / 模板同源配套配置 |
| 直接改那 `21` 个 `large_features/*.json` | 不推荐 | 会 | 这是 biome 的 `large_features` 阶段总入口 |
| 直接改 biome 本体的 `features` 列表 | 很不推荐 | 这次未必直接压你，但兼容性最差 | 属于整段接管 biome 生成链 |

如果只记一句话：

- 最稳的是“新增自己的地物，然后追加到公共标签”。
- 最容易和别的模组、数据包打架的是“直接去抢总入口或 biome 本体”。

## 三、普通 placed feature 的气候写法有扩展字段

当前项目在 `1.20` 上补了 `1.21` 风格的 `ClimatePlacement` 扩展，因此 worldgen 里的：

- `type: "tfc:climate"`

除了原版常见字段外，还支持额外字段。

### 目前可用的扩展字段

- `min_groundwater`
- `max_groundwater`
- `min_rain_variance`
- `max_rain_variance`
- `rain_variance_absolute`
- `forest_types`
- `min_elevation`
- `max_elevation`
- `ignore_rivers`

示例：

```json
{
  "feature": "yourmod:plant/my_patch",
  "placement": [
    { "type": "minecraft:rarity_filter", "chance": 5 },
    { "type": "minecraft:in_square" },
    { "type": "minecraft:heightmap", "heightmap": "WORLD_SURFACE_WG" },
    {
      "type": "tfc:climate",
      "min_temperature": 12.0,
      "max_temperature": 40.0,
      "min_groundwater": 220.0,
      "max_groundwater": 500.0,
      "min_rain_variance": -0.5,
      "max_rain_variance": 0.8,
      "rain_variance_absolute": false,
      "min_elevation": -64,
      "max_elevation": 320
    }
  ]
}
```

### 这是不是原版 `1.20 TFC` 写法

不是。

这是当前项目为了移植 `1.21` 气候 / 地物语义，在 `1.20` 上补出来的兼容写法。

因此：

- 如果你只需要原版 `1.20 TFC` 的温度 / 降雨 / 森林范围，继续写原版字段即可
- 如果你要用 `groundwater / rain_variance / elevation`，那就是本项目特例写法

## 四、生物与刷怪的正确接法

### 默认仍按原版链路走

生物侧当前默认仍然优先按原版 `1.20 TFC` 思路接入：

- `Fauna.MANAGER.register(entityId)`
- `SpawnPlacementRegisterEvent`
- biome spawner 数据

也就是说：

- 如果你是做“已有实体的 biome 刷怪分布调整”，优先沿原版 biome spawner 数据处理
- 如果你是做“已有 fauna 的气候条件调整”，优先看对应的 fauna 数据文件

### 已注册实体只用 KubeJS 加刷怪，是否可以

可以，前提是这个实体已经由它所属的 mod 正常注册。

- `KubeJS / biome modifier / 直接改 biome spawners` 这几类写法，本质上都是“往 biome 的刷怪池里追加一条已注册实体”。
- 这种接法本身不会和本项目冲突，也不要求你先把实体并进 `Fauna.MANAGER`。
- 但它默认不会自动读取 fauna 数据，也不会自动应用本项目的 `climate` 扩展字段、`months`、稳定 placement bridge。

换句话说：

- 只想让一个已注册实体按原版 / KubeJS 方式出现在哪些 biome 里，这样做没问题。
- 想让它进一步参与本项目的气候 / 月份筛选，则还要额外接 fauna 链。

### 模板 A：只把已注册实体加进 biome 刷怪

适用场景：

- 实体已经由其他 mod 注册好了
- 你只想控制它出现在哪些 biome、属于哪个刷怪分类、组大小是多少
- 你不要求它自动接入本项目的 fauna 气候筛选

KubeJS 侧建议把它理解成“给 biome 追加刷怪条目”。示例用伪代码表示，因为不同 `KubeJS / 插件` 版本事件名会不同，但字段语义是一样的：

```js
// 伪代码模板：按你当前 KubeJS / 插件版本替换事件名和具体 API
BiomeEvents.addSpawn(event => {
  event.addSpawn({
    biomes: ['tfc:plains', 'tfc:hills'],
    category: 'creature',
    entity: 'othermod:your_animal',
    weight: 12,
    minCount: 2,
    maxCount: 4
  })
})
```

这类条目里最常见的字段含义是：

- `biomes`
  - 这条刷怪规则作用到哪些 biome。
- `category`
  - 刷怪分类，例如 `creature`、`monster`、`water_creature`、`water_ambient`。
  - 这会影响它进入哪一类原版刷怪池。
- `entity`
  - 已注册实体的 id，例如 `othermod:your_animal`。
- `weight`
  - 相对权重。数值越大，只表示“在同池里被选中的相对概率更高”，不是绝对刷新率。
- `minCount`
  - 一次成组刷新的最小数量。
- `maxCount`
  - 一次成组刷新的最大数量。

这套写法只负责“往哪里尝试刷”和“刷几只”，不负责本项目的 fauna 气候判断。

### 只靠数据包能做什么

只靠数据包，适合做：

- 修改已存在实体的 biome 刷怪分布
- 修改已存在 fauna 的气候参数

只靠数据包，不适合做：

- 凭空新增一个全新的可参与 `TFC` fauna 体系的实体

因为新实体要真正并入 `TFC` 风格刷怪链，仍然需要代码侧完成：

- `Fauna.MANAGER.register(...)`
- `SpawnPlacementRegisterEvent`
- 实体自身 spawn placement 与判定桥接

### 模板 B：改已有 fauna 的数据文件

适用场景：

- 这个实体已经在 `Fauna.MANAGER` 里
- 你只是想改它的温度、湿度、森林、月份等判定
- 你不需要重写注册代码

第三方通用模板建议写成下面这种标准结构：

```json
{
  "chance": 6,
  "distance_below_sea_level": 5,
  "solid_ground": true,
  "max_brightness": 11,
  "climate": {
    "min_temperature": -5.0,
    "max_temperature": 18.0,
    "min_groundwater": 120.0,
    "max_groundwater": 420.0,
    "min_forest": 1,
    "max_forest": 4,
    "min_elevation": -64,
    "max_elevation": 220
  },
  "months": [
    "april",
    "may",
    "june",
    "july",
    "august"
  ]
}
```

这里有一个很重要的兼容提醒：

- 当前项目现在同时兼容两种 fauna 气候写法：
  - `1.20` 原版写法：放在 `climate` 子对象里。
  - `1.21` 扁平写法：直接把 `min_temperature / min_groundwater / min_elevation` 这类字段平铺在顶层。
- 但给第三方作者的推荐模板仍然优先写 `climate` 子对象，因为这和原版 `1.20 TFC`、其他数据包示例更一致，跨环境迁移更稳。
- 当前项目自带的 `src/main/resources/data/tfc/tfc/fauna/*.json` 已经大量使用 `1.21` 扁平写法，并通过 `FaunaMixin` 做了兼容恢复。

### 模板 C：让新实体真正参与本项目的 fauna 气候 / 月份刷怪

适用场景：

- 你自己的 mod 已经注册了实体
- 你希望它不只是“被 biome 刷怪池尝试刷出”，而是进一步参加 `Fauna.MANAGER`、`climate`、`months` 这些筛选

最低限度需要同时做三件事：

1. 把实体注册进 `Fauna.MANAGER`
2. 在 `SpawnPlacementRegisterEvent` 里注册 placement 与判定
3. 用 biome 数据 / KubeJS 决定它出现在哪些 biome 里尝试刷新

代码侧最小模板可以按下面这个结构写：

```java
public final class MyFaunas
{
    private static final Supplier<Fauna> MY_FAUNA = Fauna.MANAGER.register(MyEntities.MY_MOB.getId());

    public static void registerSpawnPlacements(SpawnPlacementRegisterEvent event)
    {
        event.register(MyEntities.MY_MOB.get(), SpawnPlacements.Type.ON_GROUND, Heightmap.Types.MOTION_BLOCKING_NO_LEAVES, (mob, level, heightmap, pos, rand) -> {
            final Fauna fauna = MY_FAUNA.get();
            final int seaLevel = level.getLevel().getChunkSource().getGenerator().getSeaLevel();

            if (rand.nextInt(fauna.getChance()) != 0)
            {
                return false;
            }
            if (fauna.getDistanceBelowSeaLevel() != -1 && pos.getY() > seaLevel - fauna.getDistanceBelowSeaLevel())
            {
                return false;
            }
            if (!NTEFaunaSpawnHelpers.isClimateValid(fauna, level, pos, rand) || !NTEFaunaSpawnHelpers.isMonthValid(fauna, level, pos))
            {
                return false;
            }
            if (fauna.isSolidGround() && !Helpers.isBlock(level.getBlockState(pos.below()), BlockTags.VALID_SPAWN))
            {
                return false;
            }
            return fauna.getMaxBrightness() == -1 || level.getRawBrightness(pos, 0) <= fauna.getMaxBrightness();
        }, SpawnPlacementRegisterEvent.Operation.REPLACE);
    }
}
```

补充说明：

- 陆生实体通常用 `SpawnPlacements.Type.ON_GROUND`
- 水生实体通常用 `SpawnPlacements.Type.IN_WATER`，并补上流体可生成检查
- 两栖掠食者通常用 `SpawnPlacements.Type.NO_RESTRICTIONS`

完整参考请直接对照：

- [`NTEFaunas.java`](C:/Users/g1739/Desktop/MCmod源码/群峦地形移植/src/main/java/com/newterraearth/tfe/common/entity/NTEFaunas.java)
- [`NTEVanillaFaunas.java`](C:/Users/g1739/Desktop/MCmod源码/群峦地形移植/src/main/java/com/newterraearth/tfe/common/entity/NTEVanillaFaunas.java)

这时 biome / KubeJS 侧追加刷怪，才只是“决定哪些 biome 会尝试刷新它”；真正能不能刷出来，还会继续经过 fauna 条件判断。

### fauna 数据里的字段

`fauna` 顶层最常见的字段如下：

- `chance`
  - 默认值：`1`
  - 实际判定是 `rand.nextInt(chance) == 0`。
  - `1` 表示每次检查都通过这一关；数值越大，实际越稀。
- `distance_below_sea_level`
  - 默认值：`-1`
  - 不为 `-1` 时，要求 `y <= seaLevel - 该值`。
  - 常用于要求更深水域或更低海拔才允许刷出。
- `climate`
  - 默认值：无限制。
  - 这是 `1.20` 原版推荐写法，用来承载温度、降雨、地下水、森林、海拔等条件。
- 也可以直接把气候字段平铺在顶层
  - 这是当前项目兼容的 `1.21` 写法。
  - 例如 `min_temperature / max_temperature / min_groundwater / max_groundwater / forest_types / min_elevation / max_elevation` 可以不再包一层 `climate`。
- `solid_ground`
  - 默认值：`false`
  - 为 `true` 时，脚下方块必须属于 `BlockTags.VALID_SPAWN`。
  - 一般陆生动物更常用。
- `max_brightness`
  - 默认值：`-1`
  - 不为 `-1` 时，要求当前位置亮度不高于这个值。
- `months`
  - 默认值：空列表
  - 只有空列表以外时才生效，表示只允许这些月份自然刷新。
  - 当前项目已经接通了这条判定，且使用半球感知月份。
  - 建议使用全小写英文月份名，例如 `april`、`may`、`june`。

### `climate` 里的原版通用字段

无论你把它们写在 `climate` 子对象里，还是按当前项目支持的 `1.21` 兼容写法平铺到顶层，下面这些字段的语义都一样：

下面这些字段属于 `1.20 TFC` 原版就有的通用能力：

- `min_temperature`
  - 最低允许平均温度。
- `max_temperature`
  - 最高允许平均温度。
- `min_rainfall`
  - 最低允许降雨量。
- `max_rainfall`
  - 最高允许降雨量。
- `min_forest`
  - 最低森林等级。
  - 原版可写 `none / sparse / edge / normal / old_growth`。
  - 当前项目也兼容写成 `0..4` 的密度整数，第三方模板更推荐整数，迁移更稳。
- `max_forest`
  - 最高森林等级，写法同上。
- `fuzzy`
  - 是否启用“越接近区间中心越容易通过”的模糊筛选。
  - `false` 表示硬区间，`true` 表示边缘区域更容易被刷掉。

### `climate` 里的本项目扩展字段

同样地，下面这些扩展字段既可以写在 `climate` 子对象里，也可以在当前项目中按 `1.21` 口径直接平铺到 fauna 顶层：

下面这些字段不是原版 `1.20 TFC` 通用写法，而是本项目额外补进来的：

- `min_groundwater`
  - 最低允许地下水。
- `max_groundwater`
  - 最高允许地下水。
  - 只要写了 groundwater 字段，湿度判定就会转为走地下水，不再按 `min_rainfall / max_rainfall` 解释。
- `min_rain_variance`
  - 最低允许降雨季节波动值。
- `max_rain_variance`
  - 最高允许降雨季节波动值。
  - 这表示“雨季相位 / 季节偏向”，不是当前瞬时降雨量。
- `rain_variance_absolute`
  - 是否把南北半球的正负号折叠成绝对值后再比较。
  - `false` 时，半球方向仍然参与比较。
- `forest_types`
  - 进一步限制到 `1.21` 风格的具体森林类型，而不只是 `0..4` 密度。
  - 例如可以写 `primary_diverse`、`secondary_dense`、`edge_monoculture`。
- `min_elevation`
  - 最低允许海拔 `y`。
- `max_elevation`
  - 最高允许海拔 `y`。
- `ignore_rivers`
  - 主要给 groundwater 语义用。
  - 开启后，会忽略河流对局部湿度的抬升影响，更接近基础降水背景。

### 当前项目对 fauna 侧的实际支持边界

现在这部分的正式口径应该这样理解：

- 对已经接入 `Fauna.MANAGER` 的实体：
  - `climate` 扩展字段会参与运行时判定
  - `months` 也会参与运行时判定
  - `1.20` 嵌套 `climate` 写法与 `1.21` 扁平 fauna 写法都会参与运行时判定
- 对只是在 KubeJS / biome 刷怪池里追加、但没有接入 fauna 链的实体：
  - 只会走 biome 刷怪条目本身
  - 不会自动读取 fauna 数据
- 对非常规 placement 类型实体：
  - 如果不是常规陆生、水生、两栖掠食者这三类口径，仍然可能需要代码侧显式接桥

因此，当前项目不应被理解成：

- “任何只靠 KubeJS 加进 biome 的实体，都会自动拥有完整 fauna 气候语义”

当前项目更准确的口径是：

- 原版 / KubeJS biome 刷怪仍然可用
- 想吃到本项目的 fauna 气候 / 月份增强，就额外接入 fauna 链
- 特殊 placement 的实体，必要时再补代码桥接

### 当前已经存在的生物特例

目前仍属于项目特例、需要记住的主要是两类：

#### 1. 水生生物起始高度修正

当前项目会在运行时对：

- `WATER_AMBIENT`
- `WATER_CREATURE`
- `UNDERGROUND_WATER_CREATURE`

修正随机起始 `y`，让它们适配更高、更深的 `1.21` 风格海洋柱。

这部分是运行时代码特例，不需要数据包额外声明，也不能仅靠数据包复制。

#### 2. spawn placement 稳定推导

当前项目为了避免 `SpawnPlacementRegisterEvent` 监听顺序导致的水陆分类漂移，代码侧不再在注册时临时借用已有的 `SpawnPlacements.getPlacementType(...)` 结果，而是按实体类别 / 父类稳定推导：

- `WATER_AMBIENT`
- `WATER_CREATURE`
- `UNDERGROUND_WATER_CREATURE`

以上三类统一视为 `IN_WATER`。

- `AmphibiousPredator`

这一类统一视为 `NO_RESTRICTIONS`。

- 其余并入 fauna 链的常规实体

默认视为 `ON_GROUND`。

这条规则只修正代码侧 placement bridge，不改变第三方继续沿 `Fauna.MANAGER + SpawnPlacementRegisterEvent + biome spawner + fauna JSON` 的扩展口径。
如果第三方新增的是非常规放置类型实体，仍然需要代码侧显式接桥。

## 五、推荐决策表

### 地物

- 普通地物、果树、作物、植物：优先按原版 `TFC` 标签链写
- 想并入默认森林主链：改 `data/tfe/tags/worldgen/configured_feature/forest_121_trees.json`
- 想用 `1.21` 气候语义筛选普通地物：继续写 `tfc:climate`，但使用本文列出的扩展字段

### 森林树

- 只要原版温度 / 降雨筛树：`tfc:forest_entry`
- 要接 groundwater / rain variance / elevation / soil_disc：`tfe:forest_entry_121`

### 生物

- 已注册实体只想按原版 / KubeJS 方式加到 biome 刷怪：直接改 biome 刷怪数据即可
- 已在 `Fauna.MANAGER` 里的实体，想调气候 / 月份：改 fauna 数据
- 想让新实体真正进入 `TFC` fauna 刷怪链：必须有代码注册 + fauna 数据
- 想让新实体参与本项目的气候 / 月份增强：除 biome 刷怪外，还要接 fauna 链
- 如果是非常规 placement 类型：额外补代码桥接

## 六、当前不应参考的东西

- 不要把 `AFC` 当成当前项目的通用兼容模板
- 不要把非森林果树写进 `forest_121_trees`
- 不要默认假设所有 `in_biome/<stage>/<biome>.json` 都像原版那样适合做第三方通用扩展入口

## 七、后续维护规则

后续如果本项目再新增任何“与原版 `1.20 TFC` 不同，但允许第三方使用”的数据链写法，都应继续补进本文，而不是只散落在 `开发文档.md` 或聊天记录里。
